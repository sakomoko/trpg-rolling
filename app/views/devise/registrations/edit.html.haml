%h2
  ユーザー情報の編集
%div
  = bootstrap_form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :method => :put, :class => 'form-horizontal'}) do |f|

    = f.control_group :name do
      = f.text_field :name

    = f.control_group :email do
      = f.email_field :email

    = f.control_group :password do
      = f.password_field :password
      %p.help-block
        変更しない場合は空白にしておいて下さい

    = f.control_group :password_confirmation, label: '現在のパスワード' do
      = f.password_field :password_confirmation

    = f.control_group :current_password, label: '新しいパスワード' do
      = f.password_field :current_password
      %p.help-block
        パスワードの変更の際は、変更前と、変更後のパスワードを入力する必要があります

    %div.form-actions
      = f.submit "Update", class: 'btn-warning'
  %h3 Cancel my account
  %p
    Unhappy? #{link_to "Cancel my account", registration_path(resource_name), :confirm => "Are you sure?", :method => :delete}.
  = link_to "Back", :back
