= simple_form_for [@parent, @room], html: {class: "form-horizontal"} do |f|

  = f.input :title
  = f.input :description, input_html: {class: "input-xlarge", rows: 3}, as: :text
  = f.input :static, as: :boolean
  .form-actions
    = f.submit :submit, class: 'btn btn-primary'
