= simple_form_for @game_system do |f|
  -if f.error_notification
    .alert.alert-error
      = f.error_notification

  = f.input :name

  = f.input :description, as: :text, input_html: {class: "input-xlarge", rows: 3}

  .form-actions
    = f.submit 'Save', class: 'btn btn-primary'
